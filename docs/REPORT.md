# Отчёт по работе — Make Love Adventures

**Дата:** 2026-05-22
**Бранч:** main
**Билд:** `npm run build` — PASS, 0 ошибок

---

## 1. Меню мини-игр

**Файл:** `src/scenes/DevMinigameMenuScene.ts`

- Кнопки переформатированы из 2 колонок (320×64) в **1 колонку 640×96**. Длинные названия типа «ПЕРЕПУТАННЫЕ РЕЦЕПТЫ» и «ДЖЕФФРИ ЧЕРЕЗ ДОРОГУ» теперь спокойно влезают, не вылазя за рамки.
- Шрифт поднят с 15 до **22 px**, обводка с 7 до 6 px (для баланса).
- 8 минок + кнопка «назад» равномерно делят высоту между подзаголовком и нижним краем — пустого пространства не остаётся.
- Кнопка «← НА ГЛАВНУЮ»: фон сменён с `cream` (#FAF7F0) на тёмно-индиго **`#3c3a8c`**. Раньше был белый текст с чёрной обводкой на белом фоне — нечитабельно. Теперь высокий контраст и цвет вписан в фиолетовую палитру UI.

## 2. Главное меню

**Файл:** `src/scenes/SplashScene.ts`

- Кнопки PLAY и MINI GAMES увеличены: **120 → 130 px** по высоте.
- PLAY съехала с y=735 на **y=775** — увеличено расстояние от деревянной таблички ADVENTURES.
- MINI GAMES съехала на y=925, гэп между PLAY и MINI GAMES сохранён.
- Шрифт двустрочного «MINI\\nGAMES» уменьшен с 37 до **34 px**, `lineSpacing` 10→8 — теперь текст полностью внутри центральной зоны и не залезает на градиент-рим.

## 3. ChoiceScene (экран между уровнями)

**Файлы:** `src/scenes/ChoiceScene.ts`, `src/i18n/ru.ts`

- Удалена иконка геймпада с кнопки «ИДТИ ДАЛЬШЕ» (была названа «эмодзи гитары»).
- Звезда-блик на кнопке «КРУТИТЬ КОЛЕСО» оставлена.
- **Прогресс-бар переделан**: линии между кружочками теперь литые 10 px толщиной с чёрным контуром 16 px.
  - Кружок ≤ текущего уровня → красный.
  - Линия от прошлого кружка до текущего → красная, если правый кружок завершён; иначе серая.
  - Уровень 1: только первый кружок красный, линий нет.
  - Уровень 2: первые 2 кружка + линия 1→2 красные.
  - И т.д. до уровня 4.
- Текст «Призы будут жирнее» заменён на формальное **«Награды станут ценнее»**.
- Заголовок «РАСКОЛБАС! :3» → **«УРОВЕНЬ ПРОЙДЕН»**.
- Корпус «Что делаем дальше, братишка?» → **«Выберите дальнейший шаг»**.
- Эмодзи 🎰 и 🎸 удалены из текстов CTA-кнопок.

## 4. DontWork (режь дедлайны)

**Файл:** `src/minigames/DontWork/index.ts`

- **Верхняя панель счёта**: с `add.rectangle` + stroke перешла на `drawPixelButton` — ступенчатые скруглённые края, чёрный пиксельный контур, светлая заливка `0xeeeeee`. Единый стиль с кнопками меню.
- **Легенда снизу**: ужата с 650×210 на **600×170**, тоже в пиксельном стиле через `drawPixelButton`.
- Иконки в легенде увеличены пропорционально:
  - круги: 26 → **32**
  - картинки: 44 → **56**
  - шрифт подписей: 16 → **18**
- Меньше пустоты внутри прямоугольника.
- Выравнивание HUD сохранено: сердечки, центр панели счёта и центр home-кнопки на одной линии (y=80).

## 5. Колесо фортуны

**Файлы:** `src/config/prizes.ts`, `src/i18n/ru.ts`, `src/scenes/WheelScene.ts`, `docs/WHEEL_GUIDE.md`

### Рефакторинг

- **`src/config/prizes.ts`** — переписан полностью:
  - `PRIZE_CATALOG` — катало́г 14 призов (id, цвет, иконка, тир, промокод). Расширяемый.
  - `WHEEL_BY_LEVEL` — отдельный набор 8 призов с весами на каждый уровень 1–4.
- **`src/i18n/ru.ts`** → блок `prizes:` — теперь обезличенные `prize1` … `prize14` («Приз N»).
- **`src/scenes/WheelScene.ts`** — колесо собирается через `getWheelForLevel(currentLevel)`, label берётся из `def.displayLabel` (короткое «ПРИЗ N»). Не вылазит за треугольник сектора.
- Удалён метод `pickJackpotIndex` из сцены — переехал в `prizes.ts`.

### Бизнес-логика весов

| Уровень  | Что в пуле                              | Что меняется относительно прошлого   |
| -------- | --------------------------------------- | ------------------------------------ |
| L1       | prize-1..8 (дешёвые доминируют)         | стартовый набор                      |
| L2       | prize-3..10 (prize-1/2 ушли)            | топ-2 ходовых из L1 удалены, новые prize-9/10 |
| L3       | prize-5..12 (prize-3/4 ушли)            | топ-2 ходовых из L2 удалены, новые prize-11/12 |
| L4 (jp)  | prize-7..14 (prize-5/6 ушли)            | дешёвые удалены, prize-13/14 (legendary) добавлены |

Редкие призы прошлого уровня поднимаются в весах на следующем уровне.

### Визуал

- **Указатель сверху** — пиксельный треугольник, рисуется построчно прямоугольниками (`drawPixelTriDown` с STEP=6). Чёрный контур 76×72 + жёлтое тело 60×56 со сдвигом — даёт визуально 4 px пиксельный кант.
- **Документация для админа**: `docs/WHEEL_GUIDE.md` — где править призы, как менять веса, тестовые сценарии, заметки под будущую админ-панель.

## 6. Дебаг

### Исправлено

| Что                                            | Где                                | Что было                                                         |
| ---------------------------------------------- | ---------------------------------- | ---------------------------------------------------------------- |
| Button reliability                             | `src/ui/Button.ts:80-87`           | `pointerout` отменял нажатие при микро-движении пальца           |
| Surfer hitbox (слишком чувствительный)         | `src/minigames/Surfer/index.ts:43` | `SURFER_R` 32 → **26** (43% от визуала 120×120)                  |
| Jeffrey hitbox (плотный трафик)                | `src/minigames/JeffreySurfer/index.ts:799-808` | playerHalf 0.32 → 0.28, бампер машины 0.85 → 0.75 (-12% радиуса) |

### Про «иногда кнопка домой не срабатывает»

Проанализирован поток:

1. После lose минка показывает оверлей (`DEPTH.modal=50`, **non-interactive**) и через 900-1100 мс эмитит `complete()` → MinigameRunner стартует ResultScene.
2. Home-кнопка минки на `DEPTH.ui+5=45` (под оверлеем визуально, но интерактивная). Phaser с `topOnly: true` пропускает её через non-interactive оверлей.
3. На ResultScene своя кнопка «НА ГЛАВНУЮ» (`backBtn`, через класс `Button`).

**Вердикт:** скорее всего проблема была в `Button` — `pointerout` отменял нажатие при микро-движении пальца. Это исправлено заменой на `pointerupoutside` (отменяет только при реальном отпускании пальца за пределами кнопки). Если после фикса проблема остаётся — нужен видео-репро.

---

## 7. Аудит названий и текстов

### Сильные имена минок (соответствуют концепту)

| Внутри          | Сейчас называется      | Концепт                              | Вердикт |
| --------------- | ---------------------- | ------------------------------------ | ------- |
| `FireStarter`   | ФАЕРСТАРТЕР: ПЕЧЬ      | таймер-зона достать пиццу            | OK      |
| `Surfer`        | СЁРФЕР НА ВОЛНЕ        | Flappy Bird с серфером               | OK      |
| `PizzaAssembly` | КОЛБАСКА НА НОЖАХ      | Knife Hit (метать в колбасу)         | OK      |
| `ChopChop`      | ПЕРЕТАПАЙ ДИДИ         | гонка тапов vs AI Диди               | OK      |
| `DanceBeat`     | ПОВТОРИ КОМБО          | Simon Says со стрелками              | OK      |
| `JeffreySurfer` | ДЖЕФФРИ ЧЕРЕЗ ДОРОГУ   | Crossy Road с курьером               | OK      |
| `RecipeMemo`    | ПЕРЕПУТАННЫЕ РЕЦЕПТЫ   | Memory match карточек                | OK      |
| `DontWork`      | РЕЖЕМ ДЕДЛАЙНЫ         | Fruit Ninja с офис-айтемами          | OK      |

### Имена под вопросом

- **`RhythmBattle` → «ОЛДСКУЛ vs ШОКИНГ БЛЮ»** — название отсылает к двум музыкальным группам/стилям, но игра — простой rhythm-tap. **Не отражает концепт.** Минка не в активном пуле, но если планируется — переименовать в «РИТМ-БИТВА», «ТАПАЙ В РИТМ», «БИТМЭЙКЕР».
- **`NightDelivery` → «НОЧНАЯ ДОСТАВКА»** — минка не реализована, не в пуле. Название норм для будущей реализации.
- **`FiveDollar` → «РОВНО НА $5»** — в разработке, стаб. Название норм.

### Тексты, которые стоит причесать

Файл `src/i18n/ru.ts`. Тон описан как «корешеский, дерзкий, рок-н-ролльный» — это норм для бренда, но местами скатывается в школярство.

| Где                | Что сейчас                                                                                                                                | Предложение                                                                          |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| `result.winTitle`  | НЯМКА!                                                                                                                                    | ПОБЕДА! / ОТЛИЧНО! / ВЫИГРАЛ!                                                        |
| `result.loseSub`   | Прогресс сохранён на 2 недели. Возвращайся после следующего заказа, **братишка <з**                                                       | Прогресс сохранён на 2 недели. Возвращайся после следующего заказа.                  |
| `tutorial.intro`   | **Йоу, братишка!** Прокачайся через 4 миниги, выбирай — забрать приз сейчас или идти дальше за чем-то **пожирнее**. Проиграл — прогресс зафиксен, вернёшься после следующего заказа **:3** | Пройди 4 мини-игры. После каждой — забери приз или иди дальше за более ценным. При проигрыше прогресс сохранится на 2 недели. |
| `noTicket.body`    | **Йоу, ребятки!** Жизней нема. Закажи **пиццулю** на сайте — получишь билет на новую сессию **;)**                                        | Билет закончился. Закажи пиццу на сайте — получишь новый билет на сессию.            |
| `noTicket.ctaOrder` | ЗАКАЗАТЬ **ПИЦЦУЛЮ**                                                                                                                     | ЗАКАЗАТЬ ПИЦЦУ                                                                       |
| `splash.cta`       | **ЙОУ, ПОГНАЛИ**                                                                                                                          | ПОГНАЛИ / СТАРТ                                                                      |
| `result.copied`    | СКОПИРОВАНО **^^**                                                                                                                        | СКОПИРОВАНО                                                                          |
| Заголовок i18n     | «Тон: корешеский, дерзкий…» / «Лексика: йоу, пиццуля, ребятки, братишка, кайфани, нямка, расколбас»                                       | Пересмотреть лексикон под более чистый бренд-tone                                    |

`ChoiceScene` уже причесан в задаче #3: убрал «братишка», «РАСКОЛБАС! :3», «жирнее» — заменены на формальные эквиваленты.

### Имена внутриигровых сущностей

- **«нямки» как валюта** (было в `prize-1: '+50 нямок'`) — удалено, призы стали обезличенными «Приз N». На бэке валюту лучше назвать нейтрально: «бонусы», «очки», «makelove-баллы», «pizzcoin».
- **«ДИДИ»** (имя AI-бота в ChopChop) — детское, но в контексте арт-стиля и тематики работает как имя персонажа. Оставлено.
- **«КОЛБАСКА»** в PizzaAssembly — фирменно, в стиле бренда. Оставлено.

---

## Изменённые файлы

```
src/scenes/DevMinigameMenuScene.ts      single-column 9 buttons
src/scenes/SplashScene.ts               taller buttons, smaller MINI GAMES font
src/scenes/ChoiceScene.ts               solid progress bar, gamepad icon removed
src/scenes/WheelScene.ts                per-level wheel, pixel triangle pointer
src/config/prizes.ts                    full refactor: CATALOG + WHEEL_BY_LEVEL
src/i18n/ru.ts                          choice strings, prize1..prize14
src/ui/Button.ts                        pointerout → pointerupoutside
src/minigames/DontWork/index.ts         pixel-bordered score panel + legend
src/minigames/Surfer/index.ts           SURFER_R 32→26
src/minigames/JeffreySurfer/index.ts    collision hitbox -12%
docs/WHEEL_GUIDE.md                     admin guide (новый)
docs/REPORT.md                          этот отчёт
docs/REPORT.txt                         plain-text версия отчёта
docs/REPORT.pdf                         PDF версия отчёта
```

## Финальный билд

`npm run build` (tsc + vite) — **PASS**, 0 ошибок, 0 ворнингов в коде.
Бандл-сайз warning только от Phaser core (1.2 МБ) — не наша забота.
